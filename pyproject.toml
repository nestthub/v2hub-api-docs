[project]
name = "v2hub-api-docs"
version = "1.1.2"
description = "Official documentation for the v2hub API."
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
    "zensical>=0.0.30",
]
